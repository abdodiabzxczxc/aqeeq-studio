303-  updatedBy: int("updated_by"),
304-  createdAt: timestamp("created_at").defaultNow().notNull(),
305-  updatedAt: timestamp("updated_at").defaultNow().onUpdateNow().notNull(),
306-}, (table) => [uniqueIndex("uk_event_maison_ceremony").on(table.ceremonyId)]);
307-
308-export type EventMaisonSettings = typeof eventMaisonSettings.$inferSelect;
309-
310-// أعداد نشرة أخبار مدارس العقيق. كل عدد أسبوعي يحوي صفحات A4 مرفوعة من الإدارة.
311:export const schoolNewsIssues = mysqlTable("school_news_issues", {
312-  id: int("id").autoincrement().primaryKey(),
313-  title: varchar("title", { length: 255 }).notNull(),
314-  slug: varchar("slug", { length: 128 }).notNull().unique(),
315-  issueDate: varchar("issue_date", { length: 10 }).notNull(),
316-  coverUrl: varchar("cover_url", { length: 1024 }),
317-  description: text("description"),
318-  seasonLabel: varchar("season_label", { length: 128 }).default("موسم العقيق 2026").notNull(),
319-  readingMode: varchar("reading_mode", { length: 24 }).default("pages").notNull(),
320-  watermarkUrl: varchar("watermark_url", { length: 1024 }),
321-  watermarkScale: int("watermark_scale").default(42).notNull(),
322-  watermarkOpacity: int("watermark_opacity").default(12).notNull(),
323-  watermarkPosition: varchar("watermark_position", { length: 24 }).default("center").notNull(),
324-  watermarkTint: varchar("watermark_tint", { length: 32 }).default("#d6b96a").notNull(),
325-  status: mysqlEnum("status", ["draft", "published"]).notNull().default("draft"),
326-  createdBy: int("created_by").notNull(),
327-  publishedAt: timestamp("published_at"),
328-  createdAt: timestamp("created_at").defaultNow().notNull(),
329-  updatedAt: timestamp("updated_at").defaultNow().onUpdateNow().notNull(),
330-});
331-
332-// صفحات الفلايرات A4 داخل أي عدد؛ الملف يخزن في S3 والرابط فقط في القاعدة.
333-export const schoolNewsPages = mysqlTable("school_news_pages", {
334-  id: int("id").autoincrement().primaryKey(),
335-  issueId: int("issue_id").notNull(),
336-  imageUrl: varchar("image_url", { length: 1024 }).notNull(),
337-  imageStorageKey: varchar("image_storage_key", { length: 512 }),
338-  caption: varchar("caption", { length: 255 }),
339-  pageOrder: int("page_order").notNull().default(0),
340-  createdAt: timestamp("created_at").defaultNow().notNull(),
341-});
342-
343-// نسخ القوالب التي ينشئها المدير من أي تصميم دعوة ويحتفظ بها داخل الفعالية.
344-export const invitationPresets = mysqlTable("invitation_presets", {
345-  id: int("id").autoincrement().primaryKey(),
346-  ceremonyId: int("ceremony_id").notNull(),
327-  schoolNews: router({
328-    publicList: publicProcedure.query(() => listSchoolNewsIssues("published")),
329-    monthlyBook: publicProcedure.input(z.object({ monthKey: z.string().regex(/^\d{4}-\d{2}$/) })).query(({ input }) => getSchoolNewsMonthlyBook(input.monthKey)),
330-    list: adminProcedure.query(() => listSchoolNewsIssues()),
331-    publicIssue: publicProcedure.input(z.object({ slug: z.string().min(2).max(128) })).query(({ input }) => getSchoolNewsIssueBySlug(input.slug)),
332-    issue: adminProcedure.input(z.object({ slug: z.string().min(2).max(128) })).query(({ input }) => getSchoolNewsIssueBySlug(input.slug, true)),
333:    create: adminProcedure.input(z.object({ title: z.string().trim().min(3).max(255), slug: z.string().trim().regex(/^[a-z0-9-]+$/).min(3).max(128), issueDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), coverUrl: z.string().max(1024).optional(), description: z.string().max(4000).optional(), seasonLabel: z.string().max(128).optional(), readingMode: z.enum(["pages", "spread", "scroll"]).optional(), watermarkUrl: z.string().max(1024).nullable().optional(), watermarkScale: z.number().int().min(20).max(90).optional(), watermarkOpacity: z.number().int().min(0).max(60).optional(), watermarkPosition: z.enum(["center", "top-right", "bottom-left", "bottom-right"]).optional(), watermarkTint: z.string().regex(/^#[0-9a-fA-F]{6}$/).optional() })).mutation(async ({ input, ctx }) => {
334-      const issue = await createSchoolNewsIssue({ ...input, createdBy: ctx.user.id });
335-      await logAudit({ userId: ctx.user.id, userName: ctx.user.name, action: "school_news.create", details: JSON.stringify({ title: input.title, slug: input.slug }) });
336-      return issue;
337-    }),
338:    update: adminProcedure.input(z.object({ id: z.number().int().positive(), title: z.string().trim().min(3).max(255).optional(), issueDate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).optional(), coverUrl: z.string().max(1024).nullable().optional(), description: z.string().max(4000).nullable().optional(), seasonLabel: z.string().max(128).optional(), readingMode: z.enum(["pages", "spread", "scroll"]).optional(), watermarkUrl: z.string().max(1024).nullable().optional(), watermarkScale: z.number().int().min(20).max(90).optional(), watermarkOpacity: z.number().int().min(0).max(60).optional(), watermarkPosition: z.enum(["center", "top-right", "bottom-left", "bottom-right"]).optional(), watermarkTint: z.string().regex(/^#[0-9a-fA-F]{6}$/).optional() })).mutation(({ input, ctx }) => {
339-      const { id, ...data } = input;
340-      return updateSchoolNewsIssue(id, data);
341-    }),
342-    setCover: adminProcedure.input(z.object({ issueId: z.number().int().positive(), imageUrl: z.string().min(1).max(1024), imageStorageKey: z.string().max(512).nullable().optional() })).mutation(async ({ input, ctx }) => {
343-      const issue = await setSchoolNewsCover(input.issueId, { imageUrl: input.imageUrl, imageStorageKey: input.imageStorageKey });
344-      await logAudit({ userId: ctx.user.id, userName: ctx.user.name, action: "school_news.set_cover", details: JSON.stringify({ issueId: input.issueId }) });
345-      return issue;
346-    }),
347-    addPages: adminProcedure.input(z.object({ issueId: z.number().int().positive(), pages: z.array(z.object({ imageUrl: z.string().min(1).max(1024), imageStorageKey: z.string().max(512).optional(), caption: z.string().max(255).optional() })).min(1).max(100) })).mutation(async ({ input, ctx }) => {
348-      const pages = await addSchoolNewsPages(input.issueId, input.pages);
349-      await logAudit({ userId: ctx.user.id, userName: ctx.user.name, action: "school_news.add_pages", details: JSON.stringify({ issueId: input.issueId, count: input.pages.length }) });
350-      return pages;
351-    }),
352-    deletePage: adminProcedure.input(z.object({ id: z.number().int().positive() })).mutation(({ input }) => deleteSchoolNewsPage(input.id)),
353-    updatePage: adminProcedure.input(z.object({ id: z.number().int().positive(), caption: z.string().max(255).nullable().optional(), imageUrl: z.string().min(1).max(1024).optional(), imageStorageKey: z.string().max(512).nullable().optional() })).mutation(({ input }) => {
354-      const { id, ...data } = input;
355-      return updateSchoolNewsPage(id, data);
356-    }),
1-import { useAuth } from "@/_core/hooks/useAuth";
2-import JournalPdfImporter from "@/components/JournalPdfImporter";
3:import MediaLibrary from "@/components/MediaLibrary";
4-import { JOURNAL_READING_OPTIONS, JournalReadingMode, normalizeJournalReadingMode } from "@/lib/journalReading";
5-import { getJournalIssueShareUrl } from "@/lib/journalRoutes";
6-import { trpc } from "@/lib/trpc";
7-import { ArrowDown, ArrowRight, ArrowUp, CheckCircle2, FilePlus2, ImagePlus, Loader2, Save, Settings2, Share2, Trash2 } from "lucide-react";
8-import { useEffect, useMemo, useState } from "react";
9-import { toast } from "sonner";
10-import { useLocation } from "wouter";
11-
12-type MediaChoice = { url: string; fileName: string; storageKey?: string };
13:type Target = "cover" | "add" | "replace" | "watermark" | null;
14-const today = () => new Date().toISOString().slice(0, 10);
15-
16-export default function JournalStudioPage() {
17-  const { user, isAuthenticated } = useAuth(); const [location, navigate] = useLocation(); const isAdmin = isAuthenticated && user?.role === "admin"; const utils = trpc.useUtils();
18-  const { data: issues = [], isLoading } = trpc.schoolNews.list.useQuery(undefined, { enabled: isAdmin, refetchOnWindowFocus: false });
19-  const [slug, setSlug] = useState<string | null>(null); const selectedQuery = trpc.schoolNews.issue.useQuery({ slug: slug || "__none" }, { enabled: Boolean(slug), refetchOnWindowFocus: false }); const issue = selectedQuery.data;
20-  const [creating, setCreating] = useState(false); const [pageId, setPageId] = useState<number | null>(null); const [target, setTarget] = useState<Target>(null);
21:  const [title, setTitle] = useState("النشرة الأسبوعية"); const [date, setDate] = useState(today()); const [description, setDescription] = useState(""); const [mode, setMode] = useState<JournalReadingMode>("pages"); const [caption, setCaption] = useState(""); const [watermarkUrl, setWatermarkUrl] = useState<string | null>(null); const [watermarkScale, setWatermarkScale] = useState(42); const [watermarkOpacity, setWatermarkOpacity] = useState(12); const [watermarkPosition, setWatermarkPosition] = useState("center"); const [watermarkTint, setWatermarkTint] = useState("#d6b96a");
22-  const refresh = () => { void utils.schoolNews.list.invalidate(); void utils.schoolNews.issue.invalidate(); void utils.schoolNews.publicList.invalidate(); };
23:  useEffect(() => { if (!issue) return; setTitle(issue.title); setDate(issue.issueDate); setDescription(issue.description || ""); setMode(normalizeJournalReadingMode(issue.readingMode)); setWatermarkUrl(issue.watermarkUrl || null); setWatermarkScale(issue.watermarkScale ?? 42); setWatermarkOpacity(issue.watermarkOpacity ?? 12); setWatermarkPosition(issue.watermarkPosition || "center"); setWatermarkTint(issue.watermarkTint || "#d6b96a"); setPageId((current) => issue.pages.some((page) => page.id === current) ? current : issue.pages[0]?.id || null); }, [issue]);
24-  useEffect(() => { const issueSlug = new URLSearchParams(location.split("?")[1] || "").get("issue"); if (issueSlug && issueSlug !== slug) { setCreating(false); setSlug(issueSlug); } }, [location, slug]);
25-  const activePage = useMemo(() => issue?.pages.find((page) => page.id === pageId) || issue?.pages[0], [issue, pageId]); const activeIndex = issue?.pages.findIndex((page) => page.id === activePage?.id) ?? -1;
26-  useEffect(() => { setCaption(activePage?.caption || ""); }, [activePage?.id, activePage?.caption]);
27-  const create = trpc.schoolNews.create.useMutation({ onSuccess: (created) => { toast.success("تم إنشاء العدد"); setCreating(false); setSlug(created.slug); refresh(); }, onError: (error) => toast.error(error.message) });
28-  const update = trpc.schoolNews.update.useMutation({ onSuccess: () => { toast.success("تم الحفظ"); refresh(); }, onError: (error) => toast.error(error.message) }); const setCover = trpc.schoolNews.setCover.useMutation({ onSuccess: () => { toast.success("أصبح الغلاف الصفحة الأولى"); refresh(); }, onError: (error) => toast.error(error.message) }); const publish = trpc.schoolNews.publish.useMutation({ onSuccess: () => { toast.success("تم النشر"); refresh(); }, onError: (error) => toast.error(error.message) }); const removeIssue = trpc.schoolNews.delete.useMutation({ onSuccess: () => { toast.success("تم حذف العدد"); setSlug(null); refresh(); }, onError: (error) => toast.error(error.message) });
29-  const addPages = trpc.schoolNews.addPages.useMutation({ onSuccess: () => { toast.success("أضيفت الصفحات"); refresh(); }, onError: (error) => toast.error(error.message) }); const updatePage = trpc.schoolNews.updatePage.useMutation({ onSuccess: refresh, onError: (error) => toast.error(error.message) }); const deletePage = trpc.schoolNews.deletePage.useMutation({ onSuccess: () => { toast.message("حُذفت الصفحة"); refresh(); }, onError: (error) => toast.error(error.message) }); const reorder = trpc.schoolNews.reorderPages.useMutation({ onSuccess: refresh, onError: (error) => toast.error(error.message) });
30-  const newIssue = () => { setSlug(null); setCreating(true); setTitle("النشرة الأسبوعية"); setDate(today()); setDescription(""); setMode("pages"); setWatermarkUrl(null); setWatermarkScale(42); setWatermarkOpacity(12); setWatermarkPosition("center"); setWatermarkTint("#d6b96a"); };
31:  const chooseMedia = (asset: MediaChoice) => { if (!issue || !target) return; if (target === "cover") setCover.mutate({ issueId: issue.id, imageUrl: asset.url, imageStorageKey: asset.storageKey || null }); if (target === "add") addPages.mutate({ issueId: issue.id, pages: [{ imageUrl: asset.url, imageStorageKey: asset.storageKey, caption: asset.fileName }] }); if (target === "replace" && activePage) updatePage.mutate({ id: activePage.id, imageUrl: asset.url, imageStorageKey: asset.storageKey || null }); if (target === "watermark") setWatermarkUrl(asset.url); setTarget(null); };
32-  const move = (direction: -1 | 1) => { if (!issue || activeIndex < 0) return; const next = [...issue.pages]; const destination = activeIndex + direction; if (destination < 0 || destination >= next.length) return; [next[activeIndex], next[destination]] = [next[destination], next[activeIndex]]; reorder.mutate({ issueId: issue.id, pageIds: next.map((page) => page.id) }); };
33:  const watermarkControls = issue ? <div className="border-t border-white/[.08] pt-4"><div className="text-[11px] font-black text-amber-100">علامة القارئ المائية</div><p className="mt-1 text-[10px] leading-5 text-slate-500">تظهر خلف صفحات القارئ الداكن وتتغير المعاينة مباشرة هنا.</p><div className="mt-3 grid grid-cols-2 gap-2"><button onClick={() => setTarget("watermark")} className="rounded-lg border border-amber-300/35 p-2 text-[10px] font-black text-amber-100">{watermarkUrl ? "تبديل الشعار" : "اختيار شعار"}</button><button onClick={() => setWatermarkUrl(null)} className="rounded-lg border border-white/10 p-2 text-[10px] text-slate-400">شعار الموقع</button></div><label className="mt-3 block text-[10px] text-slate-400">الموقع<select value={watermarkPosition} onChange={(event) => setWatermarkPosition(event.target.value)} className="mt-1 w-full rounded-lg border border-slate-700 bg-black/20 p-2 text-xs"><option value="center">الوسط</option><option value="top-right">أعلى اليمين</option><option value="bottom-left">أسفل اليسار</option><option value="bottom-right">أسفل اليمين</option></select></label><label className="mt-3 block text-[10px] text-slate-400">الحجم <span className="text-amber-200">{watermarkScale}%</span><input type="range" min="20" max="90" value={watermarkScale} onChange={(event) => setWatermarkScale(Number(event.target.value))} className="mt-1 w-full accent-amber-300" /></label><label className="mt-3 block text-[10px] text-slate-400">الشفافية <span className="text-amber-200">{watermarkOpacity}%</span><input type="range" min="0" max="60" value={watermarkOpacity} onChange={(event) => setWatermarkOpacity(Number(event.target.value))} className="mt-1 w-full accent-amber-300" /></label><label className="mt-3 block text-[10px] text-slate-400">لون التأثير<input type="color" value={watermarkTint} onChange={(event) => setWatermarkTint(event.target.value)} className="mt-1 h-9 w-full rounded-lg border border-slate-700 bg-black/20 p-1" /></label></div> : null;
34-  const sharePublishedIssue = async () => {
35-    if (!issue || !issue.pages.length) return;
36-    try {
37-      if (issue.status !== "published") await publish.mutateAsync({ id: issue.id });
38-      const url = getJournalIssueShareUrl(window.location.origin, issue.slug);
39-      const details = { title: issue.title, text: `اقرأ ${issue.title} من مجلة العقيق`, url };
40-      if (navigator.share) await navigator.share(details);
41-      else { await navigator.clipboard.writeText(url); toast.success("تم نشر العدد ونسخ رابط المشاركة العام"); }
42-      refresh();
43-    } catch (error) {
44-      if ((error as Error).name !== "AbortError") toast.error("تعذر إنشاء رابط المشاركة العام");
45-    }
46-  };
47-  if (!isAuthenticated || !isAdmin) return <main dir="rtl" className="grid min-h-screen place-items-center bg-[#080b12] text-slate-300">هذه المساحة لمدير مدارس العقيق.</main>;
48-  if (creating) return <main dir="rtl" className="min-h-screen bg-[#080b12] p-4 text-slate-100 md:p-8"><div className="mx-auto max-w-2xl rounded-[2rem] border border-amber-300/20 bg-[#111622] p-6"><button onClick={() => setCreating(false)} className="text-xs text-slate-400">إلغاء</button><div className="mt-4 text-[11px] font-black text-amber-300">عدد جديد</div><h1 className="mt-1 text-3xl font-black text-amber-50">أنشئ العدد أولاً</h1><div className="mt-6 grid gap-4"><label className="text-xs font-bold">العنوان<input value={title} onChange={(event) => setTitle(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-black/20 p-3 text-sm" /></label><label className="text-xs font-bold">التاريخ<input type="date" value={date} onChange={(event) => setDate(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-black/20 p-3 text-sm" /></label><label className="text-xs font-bold">وصف العدد<textarea value={description} onChange={(event) => setDescription(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-black/20 p-3 text-sm" /></label><div className="grid gap-2 sm:grid-cols-3">{JOURNAL_READING_OPTIONS.map((option) => <button key={option.id} onClick={() => setMode(option.id)} className={`rounded-xl border p-3 text-right text-xs ${mode === option.id ? "border-amber-300 bg-amber-300/10 text-amber-100" : "border-white/10 text-slate-400"}`}>{option.title}</button>)}</div><button onClick={() => create.mutate({ title, slug: `issue-${date.replaceAll("-", "")}-${Math.random().toString(36).slice(2, 6)}`, issueDate: date, description, seasonLabel: "موسم العقيق 2026", readingMode: mode })} className="rounded-xl bg-amber-300 p-3 text-sm font-black text-slate-950">إنشاء وفتح لوحة العمل</button></div></div></main>;
49-  return <main dir="rtl" className="min-h-screen bg-[#080b12] text-slate-100"><header className="flex h-16 items-center justify-between border-b border-white/10 px-4 md:px-6"><button onClick={() => navigate("/journal")} className="inline-flex items-center gap-2 text-xs font-bold text-slate-400"><ArrowRight size={15} />رف المجلة</button><div className="flex items-center gap-2 text-sm font-black text-amber-50"><Settings2 className="text-amber-300" size={18} />استوديو مجلة العقيق</div><button onClick={newIssue} className="inline-flex items-center gap-2 rounded-xl bg-amber-300 px-3 py-2 text-xs font-black text-slate-950"><FilePlus2 size={15} />عدد جديد</button></header>{issue ? <div className="flex flex-wrap items-center justify-between gap-3 border-b border-amber-300/15 bg-amber-300/[.04] px-4 py-2.5 text-[11px] md:px-6"><span className="text-slate-400">الغلاف يظهر دائماً في أول صفحة من العدد.</span><div className="flex flex-wrap items-center gap-2"><span className={`rounded-lg px-2 py-1 text-[10px] font-black ${issue.status === "published" ? "bg-emerald-300/10 text-emerald-200" : "bg-amber-300/10 text-amber-200"}`}>{issue.status === "published" ? "منشور للرابط العام" : "مسودة غير قابلة للمشاركة"}</span><button onClick={() => void sharePublishedIssue()} disabled={!issue.pages.length || publish.isPending} className="inline-flex items-center gap-2 rounded-lg border border-emerald-300/35 bg-emerald-300/10 px-3 py-2 font-black text-emerald-100 hover:bg-emerald-300 hover:text-emerald-950 disabled:opacity-40"><Share2 size={14} />{issue.status === "published" ? "مشاركة العدد" : "نشر ومشاركة"}</button><button onClick={() => setTarget("cover")} className="inline-flex items-center gap-2 rounded-lg border border-amber-300/35 bg-amber-300/[.1] px-3 py-2 font-black text-amber-100 hover:bg-amber-300 hover:text-slate-950"><ImagePlus size={14} />اختيار غلاف العدد</button></div></div> : null}
50-    <div className="grid min-h-[calc(100vh-64px)] grid-cols-1 xl:grid-cols-[250px_minmax(0,1fr)_300px]"><aside className="border-l border-white/[.08] bg-[#0d1018] p-3"><div className="text-[10px] font-black tracking-[.14em] text-amber-300">الأعداد</div><div className="mt-3 space-y-2">{isLoading ? <Loader2 className="mx-auto animate-spin text-amber-300" /> : issues.map((item) => <button key={item.id} onClick={() => setSlug(item.slug)} className={`w-full rounded-xl border p-3 text-right ${slug === item.slug ? "border-amber-300/45 bg-amber-300/10" : "border-white/[.08] bg-black/15"}`}><div className="line-clamp-1 text-xs font-black">{item.title}</div><div className="mt-1 text-[10px] text-slate-500">{item.issueDate} · {item.pageCount} صفحات</div></button>)}</div>{issue ? <><div className="mt-6 border-t border-white/[.08] pt-4 text-[10px] font-black tracking-[.14em] text-amber-300">صفحات العدد</div><div className="mt-2 rounded-xl border border-amber-300/25 bg-amber-300/[.06] p-2 text-[10px] leading-5 text-amber-100">الغلاف ثابت دائماً في الصفحة الأولى. اختره من زر «غلاف العدد» في لوحة الخصائص.</div><div className="mt-3 grid grid-cols-3 gap-2 xl:grid-cols-1">{issue.pages.map((item, index) => <button key={item.id} onClick={() => setPageId(item.id)} className={`relative overflow-hidden rounded-lg border ${activePage?.id === item.id ? "border-amber-300 ring-2 ring-amber-300/20" : "border-white/10"}`}><img src={item.imageUrl} alt="" className="aspect-[3/4] w-full object-cover" /><span className="absolute inset-x-0 bottom-0 bg-black/70 py-1 text-[9px] font-black">{index === 0 ? "غلاف" : index + 1}</span></button>)}</div><div className="mt-3 grid gap-2"><button onClick={() => setTarget("add")} className="rounded-xl border border-amber-300/35 p-2 text-[10px] font-black text-amber-100"><ImagePlus className="mx-auto mb-1" size={15} />إضافة صفحة صورة</button><JournalPdfImporter issueId={issue.id} onImported={(pages) => addPages.mutate({ issueId: issue.id, pages }, { onSuccess: (createdPages) => { if (!issue.coverUrl && createdPages[0]) setCover.mutate({ issueId: issue.id, imageUrl: createdPages[0].imageUrl, imageStorageKey: createdPages[0].imageStorageKey || null }); } })} /></div></> : null}</aside>
51:    <section className="flex min-h-[560px] flex-col items-center justify-center bg-[radial-gradient(circle_at_50%_35%,#2a2a21,transparent_42%),#0a0d14] p-4"><div className="mb-4 flex w-full max-w-[720px] items-center justify-between text-xs text-slate-500"><span>{issue?.title || "اختر عدداً"}</span><span>{activeIndex >= 0 ? `صفحة ${activeIndex + 1}` : ""}</span></div>{activePage ? <div className="relative aspect-[1/1.414] max-h-[74vh] w-[min(86vw,580px)] overflow-hidden rounded-md bg-[#f7f3ea] shadow-[0_32px_90px_rgba(0,0,0,.55)]"><img src={activePage.imageUrl} alt={activePage.caption || "صفحة المجلة"} className="relative z-10 h-full w-full object-contain" />{watermarkUrl ? <img src={watermarkUrl} alt="معاينة العلامة المائية" className={`pointer-events-none absolute z-20 object-contain opacity-70 ${watermarkPosition === "center" ? "left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2" : watermarkPosition === "top-right" ? "right-4 top-4" : watermarkPosition === "bottom-left" ? "bottom-4 left-4" : "bottom-4 right-4"}`} style={{ width: `${watermarkScale}%`, opacity: watermarkOpacity / 100, filter: `sepia(1) saturate(.7) hue-rotate(350deg)` }} /> : null}</div> : <div className="text-center"><ImagePlus className="mx-auto text-amber-300" size={40} /><p className="mt-3 text-sm text-slate-500">اختر عدداً ثم صفحة للعمل عليها.</p></div>}</section>
52:    <aside className="border-r border-white/[.08] bg-[#0d1018] p-4"><div className="text-[10px] font-black tracking-[.14em] text-amber-300">خصائص</div>{issue ? <div className="mt-4 space-y-4"><label className="block text-[11px] font-bold text-slate-300">عنوان العدد<input value={title} onChange={(event) => setTitle(event.target.value)} className="mt-2 w-full rounded-lg border border-slate-700 bg-black/20 p-2.5 text-xs" /></label><label className="block text-[11px] font-bold text-slate-300">الوصف<textarea value={description} onChange={(event) => setDescription(event.target.value)} className="mt-2 w-full rounded-lg border border-slate-700 bg-black/20 p-2.5 text-xs" /></label><div className="space-y-2">{JOURNAL_READING_OPTIONS.map((option) => <button key={option.id} onClick={() => setMode(option.id)} className={`w-full rounded-lg border p-2 text-right text-[10px] ${mode === option.id ? "border-amber-300 bg-amber-300/10 text-amber-100" : "border-white/10 text-slate-400"}`}>{option.title}</button>)}</div>{watermarkControls}<button onClick={() => update.mutate({ id: issue.id, title, issueDate: date, description, readingMode: mode, watermarkUrl, watermarkScale, watermarkOpacity, watermarkPosition: watermarkPosition as "center" | "top-right" | "bottom-left" | "bottom-right", watermarkTint })} className="flex w-full items-center justify-center gap-2 rounded-xl bg-amber-300 p-3 text-xs font-black text-slate-950"><Save size={15} />حفظ العدد والهوية</button><div className="border-t border-white/[.08] pt-4">{activePage ? <><div className="text-[11px] font-black text-amber-100">الصفحة {activeIndex + 1}</div><label className="mt-3 block text-[10px] text-slate-400">وصف الصفحة<input value={caption} onChange={(event) => setCaption(event.target.value)} onBlur={() => updatePage.mutate({ id: activePage.id, caption: caption || null })} className="mt-1 w-full rounded-lg border border-slate-700 bg-black/20 p-2 text-xs" /></label><div className="mt-3 grid grid-cols-2 gap-2"><button onClick={() => move(-1)} disabled={!activeIndex} className="rounded-lg border border-white/10 p-2 text-xs"><ArrowUp className="mx-auto" size={15} /></button><button onClick={() => move(1)} disabled={activeIndex === issue.pages.length - 1} className="rounded-lg border border-white/10 p-2 text-xs"><ArrowDown className="mx-auto" size={15} /></button><button onClick={() => setTarget("replace")} className="rounded-lg border border-white/10 p-2 text-xs">استبدال</button><button onClick={() => { if (window.confirm("حذف الصفحة؟")) deletePage.mutate({ id: activePage.id }); }} className="rounded-lg border border-rose-300/25 p-2 text-rose-200"><Trash2 className="mx-auto" size={15} /></button></div></> : null}</div><div className="border-t border-white/[.08] pt-4"><button onClick={() => publish.mutate({ id: issue.id })} disabled={!issue.pages.length} className="flex w-full items-center justify-center gap-2 rounded-xl bg-emerald-300 p-3 text-xs font-black text-emerald-950"><CheckCircle2 size={15} />نشر العدد</button><button onClick={() => { if (window.confirm("حذف العدد بكل صفحاته؟")) removeIssue.mutate({ id: issue.id, confirm: true }); }} className="mt-2 flex w-full items-center justify-center gap-2 rounded-xl border border-rose-300/30 p-3 text-xs font-black text-rose-200"><Trash2 size={15} />حذف العدد</button></div></div> : <p className="mt-4 text-xs leading-6 text-slate-500">اختر عدداً من اليسار. تظهر صفحاته في الوسط وخصائصه هنا.</p>}</aside></div><MediaLibrary open={target !== null} onClose={() => setTarget(null)} accept="image" onSelect={chooseMedia} /></main>;
53-}
client/src/components/AlaqeeqPublicHeader.tsx:7:export function AlaqeeqPublicHeader({ eyebrow, title, children, returnHome = true, showNav = true }: { eyebrow: string; title: string; children?: ReactNode; returnHome?: boolean; showNav?: boolean }) {
