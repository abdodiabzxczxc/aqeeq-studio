import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { ArrowRight, BookOpen, CalendarDays, Check, ChevronLeft, Clapperboard, Crown, ExternalLink, Film, Flag, GalleryVerticalEnd, GraduationCap, Landmark, Loader2, Map, Medal, MonitorPlay, Palette, Play, Sparkles, Star, Trophy, WandSparkles } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";
import { useLocation } from "wouter";
import { Reveal } from "@/components/ExperienceMotion";
import { VisualEditable } from "@/components/VisualEditor";

const WORLDS = [
  { id: "future-city", title: "مدينة المستقبل", subtitle: "علوم، روبوت، واختراع", icon: Sparkles, accent: "#67e8f9", gradient: "from-cyan-400/25 via-sky-500/10 to-transparent" },
  { id: "spotlight", title: "مسرح النجوم", subtitle: "مواهب، عروض، وتكريم", icon: Star, accent: "#fbbf24", gradient: "from-amber-400/30 via-orange-500/10 to-transparent" },
  { id: "heritage", title: "وطن من نور", subtitle: "هوية، قصة، وفخر", icon: Flag, accent: "#22c55e", gradient: "from-emerald-400/25 via-lime-500/10 to-transparent" },
  { id: "golden-stage", title: "رحلة الجيل", subtitle: "ختام، تخرج، وذكريات", icon: GraduationCap, accent: "#eab308", gradient: "from-yellow-300/30 via-amber-500/10 to-transparent" },
  { id: "library", title: "مكتبة الأسرار", subtitle: "قراءة، لغة، واكتشاف", icon: BookOpen, accent: "#c084fc", gradient: "from-violet-400/30 via-fuchsia-500/10 to-transparent" },
  { id: "honor-garden", title: "حديقة الإنجاز", subtitle: "تفوق، مبادرات، وشكر", icon: Trophy, accent: "#fb7185", gradient: "from-rose-400/25 via-pink-500/10 to-transparent" },
];

const DEFAULT_SCENES = ["التريلر والانتظار", "افتتاح الستارة الرقمية", "لحظة النشاط الكبرى", "مرآة الحدث", "استوديو الذكرى"];
const IDEA_LABELS = ["تريلر الفعالية", "موسم العقيق", "بوابة العالم", "مدينة المشاريع", "Alaqeeq Spotlight", "دوري التحديات", "حكاية وطن", "معرض الفن", "الستارة الرقمية", "لحظة التوحّد", "رسائل مفاجئة", "خزانة العوالم", "مخرج الفعالية", "مرآة الحدث", "استوديو الذكرى"];

function parseScenes(value?: string | null) { try { const parsed = JSON.parse(value || "[]"); return Array.isArray(parsed) && parsed.length ? parsed.map(String) : DEFAULT_SCENES; } catch { return DEFAULT_SCENES; } }

export default function AlaqeeqLivePage() {
  const { user, loading, isAuthenticated } = useAuth();
  const [, navigate] = useLocation();
  const hasAccess = isAuthenticated && ["admin", "coordinator", "auditor"].includes(user?.role ?? "");
  const canEdit = user?.role === "admin";
  const { data: events = [], isLoading } = trpc.ceremonies.list.useQuery(undefined, { enabled: hasAccess, refetchOnWindowFocus: false });
  const utils = trpc.useUtils();
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const selected = useMemo(() => events.find((event) => event.id === selectedId) || events[0], [events, selectedId]);
  const [storyLine, setStoryLine] = useState("");
  const [trailerUrl, setTrailerUrl] = useState("");
  const [memoryCoverUrl, setMemoryCoverUrl] = useState("");
  const [worldId, setWorldId] = useState("golden-stage");
  const [scenes, setScenes] = useState<string[]>(DEFAULT_SCENES);
  const [activeScene, setActiveScene] = useState(0);
  const update = trpc.ceremonies.update.useMutation({ onSuccess: () => { toast.success("تم حفظ عالم الفعالية ومشاهدها"); void utils.ceremonies.list.invalidate(); }, onError: (error) => toast.error(error.message || "تعذر حفظ إعدادات الفعالية") });

  useEffect(() => { if (!loading && !hasAccess) navigate("/"); }, [hasAccess, loading, navigate]);
  useEffect(() => { if (!selected) return; setStoryLine(selected.storyLine || ""); setTrailerUrl(selected.trailerUrl || ""); setMemoryCoverUrl(selected.memoryCoverUrl || ""); setWorldId(selected.experienceWorld || "golden-stage"); setScenes(parseScenes(selected.stageScenes)); setActiveScene(0); }, [selected?.id]);
  if (loading || isLoading) return <div className="flex min-h-screen items-center justify-center bg-[#080a10]"><Loader2 className="animate-spin text-amber-300" size={28} /></div>;
  if (!hasAccess) return null;
  const world = WORLDS.find((item) => item.id === worldId) || WORLDS[3];
  const WorldIcon = world.icon;
  const saveStudio = () => { if (!selected) return; update.mutate({ id: selected.id, experienceWorld: worldId, storyLine: storyLine || undefined, trailerUrl: trailerUrl || undefined, memoryCoverUrl: memoryCoverUrl || undefined, stageScenes: JSON.stringify(scenes) }); };

  return <VisualEditable id="live-page" tag="section" label="صفحة استوديو Alaqeeq Live" as="section" className="block"><main dir="rtl" className="min-h-screen overflow-x-hidden bg-[#080a10] text-slate-100"><VisualEditable id="live-ambient-light" tag="section" label="إضاءة خلفية الاستوديو" as="div" className={`pointer-events-none fixed inset-x-0 top-0 h-[520px] bg-gradient-to-b ${world.gradient} blur-3xl`} /><VisualEditable id="live-header" tag="section" label="رأس استوديو البث" as="div"><header className="sticky top-0 z-30 border-b border-white/[.08] bg-[#080a10]/90 backdrop-blur-xl"><div className="container flex h-16 items-center justify-between gap-3"><VisualEditable id="live-back-dashboard" tag="button" label="زر لوحة التحكم" as="button" onAction={() => navigate("/dashboard")} className="inline-flex items-center gap-2 text-xs font-bold text-slate-400 transition hover:text-amber-200"><ArrowRight size={16} />لوحة التحكم</VisualEditable><VisualEditable id="live-studio-brand" tag="section" label="هوية استوديو البث" as="div" className="flex items-center gap-3"><div className="text-left"><VisualEditable id="live-studio-eyebrow" tag="text" label="شارة Alaqeeq Live" as="div" defaultText="AL AQEEQ LIVE" className="text-[10px] font-black tracking-[.18em] text-amber-300" /><VisualEditable id="live-studio-title" tag="text" label="عنوان الاستوديو" as="div" defaultText="استوديو الفعاليات والأنشطة" className="mt-0.5 text-sm font-black text-amber-50" /></div><VisualEditable id="live-studio-icon" tag="section" label="أيقونة الاستوديو" as="div" className="rounded-xl border border-amber-300/25 bg-amber-300/10 p-2 text-amber-200"><Clapperboard size={18} /></VisualEditable></VisualEditable></div></header></VisualEditable>

    <div className="container relative py-7 md:py-10"><VisualEditable id="live-hero" tag="section" label="غلاف استوديو البث" as="section" className="overflow-hidden rounded-[2rem] border border-amber-300/20 bg-[#0d1019] p-6 shadow-2xl shadow-black/30 md:p-9"><div className="grid gap-8 lg:grid-cols-[1.2fr_.8fr]"><VisualEditable id="live-hero-copy" tag="section" label="رسالة استوديو البث" as="div"><VisualEditable id="live-hero-kicker" tag="text" label="شارة موسم العقيق" as="div" defaultText="موسم العقيق 2026" className="inline-flex items-center gap-2 rounded-full border border-amber-300/25 bg-amber-300/[.07] px-3 py-1.5 text-[11px] font-black text-amber-200">{(text) => <><WandSparkles size={14} />{text}</>}</VisualEditable><VisualEditable id="live-hero-heading" tag="text" label="عنوان استوديو البث" as="h1" defaultText="لا تنظّم نشاطاً. أخرج عالماً كاملاً." className="mt-5 max-w-3xl text-4xl font-black leading-[1.16] text-amber-50 md:text-6xl">{(text) => <>{text.split(". ")[0]}.<br /><span style={{ color: world.accent }}>{text.split(". ").slice(1).join(". ")}</span></>}</VisualEditable><VisualEditable id="live-hero-description" tag="text" label="وصف استوديو البث" as="p" defaultText="من هنا تُصمَّم القصة، التريلر، مشاهد المسرح، شاشة الحدث، والذكرى التي تبقى بعد إغلاق الستارة. كل فعالية في مدارس العقيق تأخذ عالمها الخاص." className="mt-5 max-w-2xl text-sm leading-8 text-slate-300" /><VisualEditable id="live-idea-labels" tag="section" label="أفكار الاستوديو" as="div" className="mt-7 flex flex-wrap gap-2">{IDEA_LABELS.map((label, index) => <VisualEditable key={label} id={`live-idea-${index + 1}`} tag="text" label={`فكرة الاستوديو: ${label}`} as="span" defaultText={label} className="rounded-full border border-white/[.08] bg-white/[.035] px-3 py-1.5 text-[10px] font-bold text-slate-300">{(text) => <><span className="ml-1 text-amber-300">{String(index + 1).padStart(2, "0")}</span>{text}</>}</VisualEditable>)}</VisualEditable></VisualEditable><VisualEditable id="live-world-preview" tag="section" label="معاينة العالم المختار" as="div" className="relative min-h-[260px] overflow-hidden rounded-3xl border border-white/[.08] bg-black/25 p-5"><VisualEditable id="live-world-glow" tag="section" label="إضاءة العالم المختار" as="div" className="absolute -left-12 -top-16 h-52 w-52 rounded-full blur-3xl" style={{ background: `${world.accent}33` }} /><div className="relative flex h-full flex-col justify-between"><div className="flex items-start justify-between"><div><VisualEditable id="live-world-kicker" tag="text" label="شارة العالم المختار" as="div" defaultText="العالم المختار الآن" className="text-[11px] font-black" style={{ color: world.accent }} /><VisualEditable id="live-world-title" tag="text" label="اسم العالم المختار" as="h2" defaultText={world.title} className="mt-1 text-2xl font-black text-white" /><VisualEditable id="live-world-subtitle" tag="text" label="وصف العالم المختار" as="p" defaultText={world.subtitle} className="mt-1 text-xs text-slate-400" /></div><VisualEditable id="live-world-icon" tag="section" label="أيقونة العالم المختار" as="div" className="rounded-2xl border border-white/10 bg-black/20 p-3" style={{ color: world.accent }}><WorldIcon size={26} /></VisualEditable></div><VisualEditable id="live-current-scene" tag="section" label="المشهد الحالي" as="div" className="rounded-2xl border border-white/[.08] bg-black/30 p-4"><div className="flex items-center justify-between text-[11px] text-slate-400"><VisualEditable id="live-current-scene-label" tag="text" label="تسمية المشهد الحالي" as="span" defaultText="مشهد العرض الحالي" /><VisualEditable id="live-current-scene-index" tag="text" label="ترقيم المشهد الحالي" as="span" defaultText={`${activeScene + 1} / ${scenes.length}`} className="font-black" style={{ color: world.accent }} /></div><VisualEditable id="live-current-scene-title" tag="text" label="عنوان المشهد الحالي" as="div" defaultText={scenes[activeScene] || "المشهد القادم"} className="mt-3 text-lg font-black text-amber-50" /><VisualEditable id="live-current-scene-progress" tag="section" label="تقدم مشاهد العرض" as="div" className="mt-4 h-1.5 overflow-hidden rounded-full bg-white/[.08]"><div className="h-full rounded-full transition-all" style={{ width: `${((activeScene + 1) / Math.max(scenes.length, 1)) * 100}%`, background: world.accent }} /></VisualEditable></VisualEditable></div></VisualEditable></div></VisualEditable>

      <section className="mt-7 grid gap-5 xl:grid-cols-[320px_minmax(0,1fr)]"><aside className="rounded-3xl border border-white/[.08] bg-[#0d1019] p-4"><div className="mb-3 flex items-center gap-2 text-xs font-black text-amber-100"><CalendarDays size={15} className="text-amber-300" />فصول موسم العقيق</div><div className="space-y-2">{events.length ? events.slice().sort((a, b) => (a.seasonOrder ?? 0) - (b.seasonOrder ?? 0)).map((event, index) => <button key={event.id} onClick={() => setSelectedId(event.id)} className={`w-full rounded-2xl border p-3 text-right transition ${selected?.id === event.id ? "border-amber-300/50 bg-amber-300/[.08]" : "border-transparent bg-white/[.025] hover:border-white/10"}`}><div className="flex items-center gap-3"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl bg-black/30 text-xs font-black text-amber-300">{String(index + 1).padStart(2, "0")}</span><div className="min-w-0 flex-1"><div className="truncate text-xs font-black text-slate-100">{event.title}</div><div className="mt-1 truncate text-[10px] text-slate-500">{event.eventType} · {event.ceremonyDate || "موعد يُحدد لاحقاً"}</div></div><ChevronLeft size={15} className="text-slate-600" /></div></button>) : <div className="rounded-2xl border border-dashed border-slate-700 p-5 text-center text-xs text-slate-500">أضف أول فعالية من لوحة الإدارة لتظهر في موسم العقيق.</div>}</div><button onClick={() => navigate("/dashboard?tab=events")} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl border border-amber-300/30 px-3 py-3 text-xs font-black text-amber-200 hover:bg-amber-300/[.07]"><Sparkles size={15} />إدارة الفعاليات</button></aside>

        <section className="rounded-3xl border border-white/[.08] bg-[#0d1019] p-5 md:p-6">{selected ? <><div className="flex flex-col gap-4 border-b border-white/[.08] pb-5 md:flex-row md:items-start md:justify-between"><div><div className="text-xs font-black" style={{ color: world.accent }}>غرفة إخراج الفعالية</div><h2 className="mt-1 text-2xl font-black text-amber-50">{selected.title}</h2><p className="mt-1 text-xs text-slate-500">حدّد عالم الفعالية ثم اصنع المشاهد التي ستعيشها الشاشة والضيوف والذكريات.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => navigate(`/event/${selected.id}`)} className="inline-flex items-center gap-2 rounded-xl border border-slate-700 px-3 py-2 text-xs font-bold text-slate-300 hover:border-amber-300/40 hover:text-amber-200"><ExternalLink size={14} />بوابة الفعالية</button><button onClick={() => navigate(`/workspace/${selected.id}`)} className="inline-flex items-center gap-2 rounded-xl bg-amber-300 px-3 py-2 text-xs font-black text-amber-950"><MonitorPlay size={14} />مساحة التشغيل</button></div></div>
          <div className="mt-6 grid gap-6 2xl:grid-cols-[.9fr_1.1fr]"><div className="space-y-5"><div><div className="flex items-center gap-2 text-xs font-black text-slate-200"><Palette size={15} style={{ color: world.accent }} />خزانة العوالم</div><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-3">{WORLDS.map((item) => { const Icon = item.icon; const active = worldId === item.id; return <button key={item.id} onClick={() => setWorldId(item.id)} disabled={!canEdit} className={`rounded-2xl border p-3 text-right transition ${active ? "border-amber-300/60 bg-white/[.06]" : "border-white/[.07] bg-black/20 hover:border-white/20"} disabled:cursor-default`}><Icon size={16} style={{ color: item.accent }} /><div className="mt-3 text-[11px] font-black text-slate-100">{item.title}</div><div className="mt-1 text-[9px] leading-4 text-slate-500">{item.subtitle}</div></button>; })}</div></div><label className="block text-xs font-bold text-slate-300">جملة بوابة العالم<textarea value={storyLine} onChange={(event) => setStoryLine(event.target.value)} disabled={!canEdit} rows={4} maxLength={4000} placeholder="مثال: هنا تتحول أفكار طلاب العقيق إلى مدينة من المستقبل." className="mt-2 w-full resize-y rounded-2xl border border-slate-700 bg-black/20 px-3 py-3 text-sm leading-7 text-white outline-none transition focus:border-amber-300 disabled:opacity-65" /></label><label className="block text-xs font-bold text-slate-300">رابط تريلر النشاط أو الفيديو<input value={trailerUrl} onChange={(event) => setTrailerUrl(event.target.value)} disabled={!canEdit} dir="ltr" placeholder="https://..." className="mt-2 w-full rounded-xl border border-slate-700 bg-black/20 px-3 py-3 text-sm text-white outline-none transition focus:border-amber-300 disabled:opacity-65" /></label><label className="block text-xs font-bold text-slate-300">غلاف بوابة الذكريات<input value={memoryCoverUrl} onChange={(event) => setMemoryCoverUrl(event.target.value)} disabled={!canEdit} dir="ltr" placeholder="رابط صورة من مكتبة الوسائط" className="mt-2 w-full rounded-xl border border-slate-700 bg-black/20 px-3 py-3 text-sm text-white outline-none transition focus:border-amber-300 disabled:opacity-65" /></label>{canEdit ? <button onClick={saveStudio} disabled={update.isPending} className="flex w-full items-center justify-center gap-2 rounded-2xl px-4 py-3 text-sm font-black text-slate-950 disabled:opacity-50" style={{ background: world.accent }}><Check size={16} />{update.isPending ? "جارٍ حفظ الاستوديو…" : "حفظ عالم ومشاهد الفعالية"}</button> : null}</div>
            <div className="space-y-5"><div className="rounded-3xl border border-white/[.08] bg-black/20 p-4"><div className="flex items-center justify-between"><div className="flex items-center gap-2 text-xs font-black text-amber-100"><Clapperboard size={15} />مخرج الفعالية</div><span className="text-[10px] text-slate-500">رتّب مشاهد الحفل</span></div><div className="mt-4 space-y-2">{scenes.map((scene, index) => <div key={`${scene}-${index}`} className={`flex items-center gap-2 rounded-xl border p-2 transition ${activeScene === index ? "border-amber-300/50 bg-amber-300/[.07]" : "border-white/[.06] bg-white/[.02]"}`}><button onClick={() => setActiveScene(index)} className="flex h-7 w-7 shrink-0 items-center justify-center rounded-lg bg-black/30 text-[10px] font-black" style={{ color: activeScene === index ? world.accent : "#94a3b8" }}>{String(index + 1).padStart(2, "0")}</button><input value={scene} onChange={(event) => setScenes((current) => current.map((value, sceneIndex) => sceneIndex === index ? event.target.value : value))} disabled={!canEdit} className="min-w-0 flex-1 bg-transparent px-1 text-xs font-bold text-slate-200 outline-none disabled:cursor-default" />{canEdit && scenes.length > 1 ? <button onClick={() => setScenes((current) => current.filter((_, sceneIndex) => sceneIndex !== index))} className="rounded-lg px-2 py-1 text-xs text-slate-500 hover:bg-rose-400/10 hover:text-rose-300">×</button> : null}</div>)}</div>{canEdit ? <button onClick={() => setScenes((current) => [...current, "مشهد جديد من عالم العقيق"])} className="mt-3 w-full rounded-xl border border-dashed border-amber-300/30 py-2 text-[11px] font-black text-amber-200 hover:bg-amber-300/[.06]">إضافة مشهد</button> : null}</div><div className="overflow-hidden rounded-3xl border border-white/[.08] bg-[#090b11]"><div className="border-b border-white/[.07] px-4 py-3 text-[11px] font-black text-slate-400">مرآة الحدث — معاينة العرض</div><div className="relative min-h-[230px] overflow-hidden p-6"><div className={`absolute inset-0 bg-gradient-to-br ${world.gradient}`} /><div className="relative flex h-full min-h-[190px] flex-col justify-between"><div className="flex items-center justify-between"><span className="rounded-full bg-black/25 px-3 py-1 text-[10px] font-black" style={{ color: world.accent }}>{world.title}</span><span className="text-[10px] text-slate-500">المشهد المباشر</span></div><div><div className="text-xs text-slate-400">الآن على المسرح</div><div className="mt-2 text-3xl font-black leading-tight text-white">{scenes[activeScene]}</div><p className="mt-3 max-w-md text-xs leading-6 text-slate-300">{storyLine || "اكتب جملة بوابة عالم الفعالية لتظهر كافتتاح أنيق في شاشة الحدث."}</p></div><div className="flex items-center gap-2 text-[10px] text-slate-400"><Play size={13} fill="currentColor" style={{ color: world.accent }} />Alaqeeq Live · المشهد {activeScene + 1}</div></div></div></div></div></div></> : <div className="flex min-h-[480px] flex-col items-center justify-center text-center"><Landmark className="text-amber-300" size={34} /><h2 className="mt-4 text-xl font-black text-amber-50">ابدأ بإضافة أول فعالية</h2><p className="mt-2 max-w-sm text-sm leading-7 text-slate-500">بمجرد إضافة فعالية ستظهر هنا كفصل من موسم العقيق ويمكنك اختيار عالمها وإخراج مشاهدها.</p></div>}</section>
      </section>
      <section className="mt-6 grid gap-4 md:grid-cols-3"><Feature icon={Film} title="التريلر" body="أطلق التشويق قبل النشاط من صفحة انتظار وهوية متحركة." /><Feature icon={GalleryVerticalEnd} title="استوديو الذكرى" body="أغلق الستارة بصفحة ختامية تحفظ الصور واللحظة الأهم." /><Feature icon={Map} title="مدينة النشاط" body="حوّل المعرض أو التحدي إلى مسار بصري قابل للاكتشاف." /></section>
    </div>
  </main></VisualEditable>;
}

function Feature({ icon: Icon, title, body }: { icon: React.ElementType; title: string; body: string }) {
  const id = `live-feature-${title.replace(/\s+/g, "-")}`;
  return <Reveal><VisualEditable id={id} tag="section" label={`ميزة الاستوديو: ${title}`} as="article" className="aq-motion-card rounded-3xl border border-white/[.08] bg-[#0d1019] p-5"><VisualEditable id={`${id}-icon`} tag="section" label={`أيقونة ${title}`} as="div"><Icon size={20} className="text-amber-300" /></VisualEditable><VisualEditable id={`${id}-title`} tag="text" label={`عنوان ميزة ${title}`} as="h3" defaultText={title} className="mt-4 font-black text-amber-50" /><VisualEditable id={`${id}-body`} tag="text" label={`وصف ميزة ${title}`} as="p" defaultText={body} className="mt-2 text-xs leading-6 text-slate-500" /></VisualEditable></Reveal>;
}
