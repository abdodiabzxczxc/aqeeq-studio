# نتائج التحقق البصري

- الصفحة الرئيسية العامة تعمل وتعرض هوية المنصة والتنقل العلوي وقسم المزايا بصورة سليمة لغير المسجلين.
- المسار المحمي `/dashboard` يعيد غير المسجل إلى الصفحة الرئيسية كما هو متوقع.
- صفحة المشاركة العامة `/event/1` تعمل وتعرض هوية الفعالية وبياناتها ورمز المشاركة.
- المساحات الداخلية `/workspace/:id` تتطلب جلسة مصرحاً لها؛ سيتطلب تحققها النهائي الدخول بحساب مدير أو منسق أو مراجع.

## تحقق إعادة التنظيم

تمت مراجعة نقاط التنقل الرئيسية. بطاقات الصفحة الرئيسية ولوحة التحكم تقود إلى `/workspace/:id`، ومساحة الفعالية تعود إلى «كل الفعاليات»، بينما تفتح بوابة المسح بسياق `ceremonyId` وتعيد المستخدم إلى المساحة نفسها. تظل `/dashboard` و`/scan` مسارات عامة مقصودة لإدارة المنصة أو تشغيل البوابة عند عدم اختيار فعالية.

تحقق العرض في جلسة المعاينة من الصفحة الرئيسية بعد تسجيل الدخول، ومساحة الفعالية `/workspace/1`، وبوابة المسح `/scan?ceremonyId=1`. ظهرت مساحة الفعالية بالتنقل العلوي، والمقاييس الحية، وإجراءات الضيوف والتشغيل، كما ظهرت بوابة المسح باسم الفعالية ومؤشرات حضورها. واجهة `/dashboard` تتطلب جلسة مصرحاً لها، ويعيد المسار غير المصرح به إلى الصفحة الرئيسية حسب التصميم.

اجتاز المشروع فحص TypeScript، والبناء الإنتاجي، و22 اختبار وحدة بعد إضافة اختبارات تمرير معرّف الفعالية إلى التصدير والمسح والسجلات والإشعارات.

## واجهة الفعاليات وتتبّع البوابات — 2026-08-15

- اجتاز المشروع فحص TypeScript و27 اختبار Vitest والبناء الإنتاجي بعد إضافة خط مراحل الفعالية ومركز التشغيل الحي وتتبّع البوابات.
- حاولت لقطة المعاينة لمساري `/scan?ceremonyId=1` و`/workspace/1`؛ ظلت اللقطتان عند شاشة التحميل بسبب غياب جلسة OAuth المصرح بها في بيئة اللقطات، ولذلك لا تُعامل اللقطة كدليل على فشل الواجهة أو نجاحها.
- تغطي اختبارات الخادم تمرير اسم البوابة من مسار QR إلى طبقة المعالجة. ويظل اختبار الكاميرا التفاعلي النهائي بحاجة إلى جلسة مستخدم موظف استقبال فعلية.
